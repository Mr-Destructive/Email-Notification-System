{% extends "base.html" %}
{% block body %}
{% load static %}

<link rel="stylesheet" href="{% static 'mail/style.css' %}">
        <form action="results.html" method="POST" enctype="multipart/form-data">
		{% csrf_token %}
          <!-- Subject -->

            <div>
              <label id="subject" for="name">Subject</label>
              <input id ="subjectfield" type="text" name="subject" id="name" required>
          </div>

	  <div>
		  <label id="sender">From: </label>
		  <label id="sendid"> {{ user.email }}</label>
	  </div>

         <!-- message main body -->

          <div>
            <label id="body" for="name">Body: </label>
            <textarea type="text" name="name" id="messageBody" required></textarea>
        </div>

        <!-- Recipient list -->
          <label > 
		  <input type="email" id ="recipientlabel" placeholder="Recipient Mails" multiple />
		  </label>

          <label for="upload" id="file"> 
	  	<input type="file" id="upload" multiple hidden />
		  <img src="{% static 'img/attachment.jpeg' %}" class="attachment" alt="attachment">
		  </label>

        <!-- Time -->

          <input id= "time" type="time" id="appt" name="appt">

        <!-- Date -->
        <div>
          
          <input type="date" name="date" id="date">
        </div>

        <!-- Send Button -->
        <div>
        <button id="send" type="submit">Send</button>


        </form>
{% endblock body %}
